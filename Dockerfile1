# obtenemos la imagen
FROM debian:buster
MAINTEINER jelou

RUN apt update
RUN apt install -y git nvim emacs irssi
EXPOSE 6667

# cmd /usr/sbin/apache2ctl -D foreground
#
# docker build -t nombreNuevaImagen:tag "ruta del dockerfile"
# docker run nombreImagen
# docker run -d -p 8000:80 nombreNuevaImagen
#
# docker run -d -p 8080:80 --network mired --name miservidor nginx
# docker network connect mired miservidor
